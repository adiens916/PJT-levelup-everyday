import styled from '@emotion/styled';
import { CircularProgress, Typography } from '@mui/material';

export const LoadingCircle = styled(CircularProgress)({
  display: 'block',
  marginLeft: 'auto',
  marginRight: 'auto',
});

export const TypographyCentered = styled(Typography)({
  display: 'block',
  marginLeft: 'auto',
  marginRight: 'auto',
});
